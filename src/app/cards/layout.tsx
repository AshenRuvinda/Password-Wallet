import { ReactNode } from 'react';

export const metadata = {
  title: 'Cards - Password & Credit Card Wallet',
  description: 'Manage your credit cards securely',
};

export default function CardsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}