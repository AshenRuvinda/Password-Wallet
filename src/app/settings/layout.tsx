import { ReactNode } from 'react';

export const metadata = {
  title: 'Settings - Password & Credit Card Wallet',
  description: 'Update your profile settings',
};

export default function SettingsLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}